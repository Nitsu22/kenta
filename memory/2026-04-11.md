- kentaサイトの更新作業を継続。招待サイトの構成をかなり整理して、HOST/GALLERY削除、SCHEDULE簡略化、Dress Code の見やすい案内化、送迎バス案内の整理、フォーム項目の大幅削減を実施。
- 送迎バス案内には空港内案内画像 `assets/images/shuttle-meeting-point.jpg` を追加し、画像とキャプションをタップで拡大できるようにした。
- トップのメイン画像を `assets/images/hero-photo-main.jpg` に差し替え、ヒーロー文字は上と下に分けて中央を空けるレイアウトへ変更した。
- git push の不具合を調査。認証は `credential.helper=osxkeychain` 経由で通っており、GitHub への HTTPS 接続/TLS も成功しているが、`git-receive-pack` 後に `send-pack: unexpected disconnect while reading sideband packet` / `HTTP 400` が出て push 最終応答で切断されることを確認。主因は認証ではなく HTTPS push の最終応答切断と判断。
